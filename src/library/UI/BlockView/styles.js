import {Theme} from '../../Theme';

export default theme => ({
	view: {
		position: 'absolute',
		left: 0,
		right: 0,
		top: 0,
		bottom: 0,
		backgroundColor: theme.color.WHITE,
		opacity: 0,
	},
});
