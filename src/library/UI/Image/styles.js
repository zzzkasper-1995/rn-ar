export default theme => ({
	image: {
		width: 90,
		height: 72,
		// backgroundColor: theme.color.GRAY_DARK,
	},
	text: {
		fontWeight: theme.simple.fontWeight.medium,
		fontSize: 18,
		lineHeight: 20,
		color: theme.color.GRAY_BORDER_LIGHT,
	},
	reserveView: {
		justifyContent: 'center',
		alignItems: 'center',
		backgroundColor: theme.color.GRAY_DARK,
	},
});
